# 203dd7cfbc2be3d6c1fb20ad68beb315f1902ca451f0d326efe92832dc4a5649 Elan 1.0.3 guest default_profile valid

main
  let t be new Turtle()
  call t.placeAt(0, 0)
  call t.turnToHeading(90)
  call t.show()
  for i from 75 to 0 step -1
    call t.move(i)
    call t.turn(90)
    call pause(300)
  end for
end main
