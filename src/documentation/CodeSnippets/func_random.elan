# 2c49e2769249e4c945a433c2cb39d3174567b550c7572e4b22b32afb51554e7f Elan 2.0.0-alpha guest default_profile valid

main
  variable rnd set to new Random()
  call rnd.initialiseFromClock()
  variable dice set to 0
  for i from 1 to 10 step 1
    # set dice,rnd to rollDice(rnd) Invalid.?? 
    call print(dice)
  end for
end main

function rollDice(rnd as Random) returns (Int, Random)
  return rnd.nextInt(1, 6)
end function
