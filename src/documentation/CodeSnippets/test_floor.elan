# b3a108e44d4daea36d806ee085f2c450fb0dba793eacf8e77078b85346073880 Elan 2.0.0-alpha guest default_profile valid

test test_floor
  variable p set to pi
  variable mp set to -pi
  assert p.floor() is 3
  assert mp.floor() is -4
  assert p.ceiling() is 4
  assert mp.ceiling() is -3
  assert p.round(3) is 3.142
  assert mp.round(2) is -3.14
  assert sqrt(-p).isNaN() is true
  variable q set to 1/0
  assert q.isInfinite() is true
end test
