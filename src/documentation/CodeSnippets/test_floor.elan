# fb72dd43b0c51df7570fc00d826cb24d822b3155c11a1e4ba16d8ff8e4fed4dc Elan 1.9.0 guest default_profile valid

test test_floor_etc
  let p be pi
  let mp be -pi
  assert p.floor() is 3
  assert mp.floor() is -4
  assert p.ceiling() is 4
  assert mp.ceiling() is -3
  assert p.round(3) is 3.142
  assert mp.round(2) is -3.14
  assert sqrt(-p).isNaN() is true
  let q be 1/0
  assert q.isInfinite() is true
end test
