# 4726425e8654e0c7ce617fa790dfb9393326cbad9ce40605ee2b5381c8cb4bc1 Elan 2.0.0-alpha guest default_profile valid

test test_floor
  let p be pi
  let mp be -pi
  assert p.floor() is 3
  assert mp.floor() is -4
  assert p.ceiling() is 4
  assert mp.ceiling() is -3
  assert p.round(3) is 3.142
  assert mp.round(2) is -3.14
  assert sqrt(-p).isNaN() is true
  let q be 1/0
  assert q.isInfinite() is true
end test
