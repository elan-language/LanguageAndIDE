# 1c7fbf0a0e4826b1fc511b18ec610bc6565769d284961c8557770a780dc36639 Elan 1.9.0 guest default_profile valid

test 
    let p be pi
    let mp be -pi
    assert p.floor() is 3
    assert mp.floor() is -4
    assert p.ceiling() is 4
    assert mp.ceiling() is -3
    assert p.round(3) is 3.142
    assert mp.round(2) is -3.14
    assert sqrt(-p).isNaN() is true
    let q be 1/0
    assert q.isInfinite() is true
end test
