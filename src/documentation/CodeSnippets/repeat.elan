# b31bcd6a4eb279df6c8aba81900e18f290cab68278f0f7bd21abd3cff8d8e6dc Elan 2.0.0-alpha guest default_profile valid

main
  let names be ["Abel", "Boole", "Cantor", "Descartes"]
  let file be createFileForWriting("Maths.txt")
  variable i set to 0
  while i < names.length()
    call file.writeLine(names[i])
    set i to i + 1
  end while
  call file.saveAndClose()
end main
