# 8ac6620ed4de45f273ae22dda26c003b388be35d0a172481256d2a9b05e40a7c Elan 1.9.0 guest default_profile valid

main
  variable st set to new Set<of Int>()
  set st to st.addFromList([3, 5, 7])
  call printLine(st.length())
  set st to st.add(7)
  call printLine(st.length())
  set st to st.remove(3)
  call printLine(st.length())
  set st to st.remove(3)
  call printLine(st.length())
  call printLine(st)
end main
