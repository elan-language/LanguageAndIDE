# dc92a37b8345b1c3fa02944454e93402cff247048e0b6e8b2efe48df6ec20c5d Elan 1.9.0 guest default_profile valid

main
  variable st set to new Set<of Int>()
  set st to st.addFromList([3, 5, 7])
  call printLine(st.length())
  set st to st.add(7)
  call printLine(st.length())
  set st to st.remove(3)
  call printLine(st.length())
  set st to st.remove(3)
  call printLine(st.length())
  call printLine(st)
end main
