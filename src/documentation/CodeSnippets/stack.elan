# 36b6b9218a64c21a65f33cdffef817d614f5724b48e4934ab4bf3a0d6cea382d Elan 1.9.0 guest default_profile valid

main
  variable st set to new Stack<of String>()
  call printLine(st.length())
  set st to st.push("apple")
  set st to st.push("Pear")
  call printLine(st.length())
  call printLine(st.peek())
  variable fruit set to ""
  set fruit, st to st.pop()
  call printLine(fruit)
  set fruit, st to st.pop()
  call printLine(fruit)
  call printLine(st.length())
end main
