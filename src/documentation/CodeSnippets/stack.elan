# 002c6ad0f1961d556a642e51462bd4ced13926287732e389dfdb568a278fa6a9 Elan 1.9.0 guest default_profile valid

main
  variable st set to new Stack<of String>()
  call print(st.length())
  set st to st.push("apple")
  set st to st.push("Pear")
  call print(st.length())
  call print(st.peek())
  variable fruit set to ""
  set fruit, st to st.pop()
  call print(fruit)
  set fruit, st to st.pop()
  call print(fruit)
  call print(st.length())
end main
