# a2ab6d3445c6d8f4e5790a037f007a7a0ee8a88d6a2212b9115e64d3da0d26e3 Elan 1.9.0 guest default_profile valid

main
  let names be ["Tom", "Dick", "Harriet"]
  call print(names[0])
  each n in names
    call print(n)
  end each
  call print(reverse("Harriet"))
end main

function reverse(s as String) returns String
  variable sReturn set to empty String
  each ch in s
    set sReturn to ch + sReturn
  end each
  return sReturn
end function
