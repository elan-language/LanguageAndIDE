# 93d58f6c876a476b6992c9607fabe308ce4ba18dc0d4c58baae645447618f451 Elan 1.9.0 guest default_profile valid

main
  let names be ["Tom", "Dick", "Harriet"]
  call printLine(names[0])
  each n in names
    call printLine(n)
  end each
  call printLine(reverse("Harriet"))
end main

function reverse(s as String) returns String
  variable sReturn set to empty String
  each ch in s
    set sReturn to ch + sReturn
  end each
  return sReturn
end function
