# 0c3b50314b2ea7f7c750ef3782dd14774b082e59147dc54d87de326e9f2ace73 Elan 2.0.0-alpha guest default_profile valid

main
  variable qu set to new Queue<of String>()
  call print(qu.length())
  set qu to qu.enqueue("apple")
  set qu to qu.enqueue("Pear")
  call print(qu.length())
  call print(qu.peek())
  variable fruit set to ""
  set fruit, qu to qu.dequeue()
  call print(fruit)
  set fruit, qu to qu.dequeue()
  call print(fruit)
  call print(qu.length())
end main
