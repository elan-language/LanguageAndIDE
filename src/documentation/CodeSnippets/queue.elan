# f8f4fec5f27c7c2c55b951f172a89756d19c17907232cd5f21bde6b12015802a Elan 1.9.0 guest default_profile valid

main
  variable qu set to new Queue<of String>()
  call print(qu.length())
  set qu to qu.enqueue("apple")
  set qu to qu.enqueue("Pear")
  call print(qu.length())
  call print(qu.peek())
  variable fruit set to ""
  set fruit, qu to qu.dequeue()
  call print(fruit)
  set fruit, qu to qu.dequeue()
  call print(fruit)
  call print(qu.length())
end main
