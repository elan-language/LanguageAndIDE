# b80fa638206097e75593e954c35aa0dd306f19f9fdd4ec3f39bc3ccc8e191e68 Elan 1.9.0 guest default_profile valid

main
  variable qu set to new Queue<of String>()
  call printLine(qu.length())
  set qu to qu.enqueue("apple")
  set qu to qu.enqueue("Pear")
  call printLine(qu.length())
  call printLine(qu.peek())
  variable fruit set to ""
  set fruit, qu to qu.dequeue()
  call printLine(fruit)
  set fruit, qu to qu.dequeue()
  call printLine(fruit)
  call printLine(qu.length())
end main
