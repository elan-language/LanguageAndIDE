# dfdbb4a8aa820bce554d896ab73d5ba48dbbd7cf8e7871cba9a008111ebec008 Elan 1.6.4 guest default_profile valid

main
  for n from 50 to 72 step 1
    variable s set to lambda n as Int => n.asString().length()
    variable np set to 2^n
    call printTab(-s(n), "{n}")
    call printTab(30 - s(np), "{np}\n")
  end for
end main
