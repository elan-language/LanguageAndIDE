# FFFF Elan Beta 6 valid

main
  variable t set to new Turtle()
  call t.placeAt(0, 0)
  call t.turnToHeading(90)
  call t.show()
  for i from 75 to 0 step -1
    call t.move(i)
    call t.turn(90)
    call t.pause(300)
  end for
end main
