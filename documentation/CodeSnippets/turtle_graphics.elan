# FFFF Elan Beta 8 valid

main
  let t be new Turtle()
  call t.placeAt(10, 10)
  call t.show()
  for i from 1 to 4 step 1
    call t.turn(90)
    call t.move(40)
    call pause(500)
  end for
end main
